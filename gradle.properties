# GraalVM CE 17.0.9 的 JIT(JVMCI) 会偶发 SIGSEGV（x86_64 与 arm64 都会触发）。
# 让 Gradle 守护进程、Kotlin 编译守护进程都改用标准 C2 JIT；
# native-image 的 AOT 编译走独立进程，不受影响。
org.gradle.jvmargs=-XX:-UseJVMCICompiler -Xmx2g
kotlin.daemon.jvmargs=-XX:-UseJVMCICompiler
